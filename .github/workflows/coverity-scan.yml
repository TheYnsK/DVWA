name: Coverity Scan

on:
  push:
    branches: [ main ]

jobs:
  coverity-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Coverity Scan
        uses: vapier/coverity-scan-action@v1
        with:
          project: 230290127_DVWATest
          token: ${{ secrets.COVERITY_SCAN_TOKEN }}
          email: theynsk.2244@gmail.com
